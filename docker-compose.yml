services:
  app:
    build: .
    container_name: babycry_api
    ports:
      - "7860:7860"
    restart: always
    environment:
      - ENV=production
      - API_KEY=${API_KEY}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-*}
      - INFERENCE_TIMEOUT_S=30
